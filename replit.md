# Bio-Magic – חדר בריחה ביו-מימטיקה

## סקירה
משחק חדר בריחה מבוסס דפדפן עם ממשק ניהול מדריך. כל הלוגיקה רצה ב-Node.js אחד ללא תלויות חיצוניות.

## ארכיטקטורה
- **שרת:** `server.js` – שרת HTTP טהור (ללא express), פורט 5000, host 0.0.0.0
- **פרונטאנד:** קבצים סטטיים תחת `public/` ו-`index.html` בשורש
- **נתונים:** `data/` – קבצי JSON עם תוכן המשחק
- **מצב:** In-memory store (שחקנים, ניקוד, סטטוס משחק) – לא נשמר לדיסק

## API Routes
- `GET /api/status` – סטטוס משחק
- `GET /api/scores` – רשימת שחקנים (לאדמין)
- `POST /api/start` – התחלת משחק (קוד אדמין: BIO2026)
- `POST /api/end` – סיום משחק
- `POST /api/reset` – איפוס
- `POST /api/join` – הצטרפות שחקן
- `POST /api/score` – עדכון ניקוד

## הרצה
```bash
node server.js
```

## Deployment
- Target: **vm** (כי יש in-memory state)
- Run: `node server.js`

## Hybrid Classroom Flow Notes (2026-04-05)
- נקודת חיבור חדשה לפני תחילת המסע: `game_setup_guide` (הזנת מספר משתתפים ל־global state בלבד).
- נקודת חיבור חדשה אחרי פתיחת דלת 1: `hybrid_group_discovery` (חלוקה מאוזנת ל־5 בתי גידול).
- שלב 4 הותאם בתוך `step_5_nature_invention_match` למסלול היברידי עם 7 צמדים לכל בית גידול, משפט סודי וגייט "קיבלנו את הקלף – המשך".
- מסך חדש `instructor_dashboard` מציג מצב קבוצות מלא (חלוקה, קודים, משפטים, התקדמות ואישורי מעבר).

### Checklist בדיקות ידניות
- [x] חלוקה מאוזנת לפי N משתתפים (הפרש מקסימלי 1 בין קבוצות, בסדר קבוע).
- [x] לכל בית גידול מוצגים בדיוק 7 צמדים וקודי `fullCode` (string עם אפסים מובילים).
- [x] לא נחשף משפט סודי לפני השלמת 7/7.
- [x] לא ניתן להמשיך לשלב הבא בלי "קיבלנו את הקלף – המשך".
- [x] לאחר אישור קלף, הזרימה חוזרת למסלול הקיים (חיפוש פריטים → דלת 2 → כתב סתרים → סיום).
